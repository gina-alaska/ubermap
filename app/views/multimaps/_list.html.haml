%table.table.table-striped.table-condensed
  %thead
    %tr
      %th Name
      %th Permalink
      %th.col-sm-1 # Maps
      %th.text-right{:colspan => "3"} Actions
  %tbody
    - multimaps.each do |multimap|
      %tr
        %td= multimap.name
        %td= link_to viewer_url(multimap), viewer_url(multimap)
        %td= multimap.maps.count
        %td.text-right
          = link_to [@organization, multimap], class: 'btn btn-sm btn-primary' do
            Manage
            %i.fa.fa-arrow-right
