%table.table.table-striped.table-condensed
  %thead
    %tr
      %th Name
      %th # Maps
      %th.text-right Active
      %th.text-right{:colspan => "3"} Actions
  %tbody
    - multimaps.each do |multimap|
      %tr
        %td= multimap.name
        %td= multimap.maps.count
        %td.text-right= multimap.active
        %td.text-right.col-sm-2
          = link_to [@organization, multimap], class: 'btn btn-primary' do
            Manage
            %i.fa.fa-arrow-right
