%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Layers
      %th.text-right Actions
  %tbody
    - maps.find_each do |map|
      %tr
        %td= map.title
        %td= map.geojson_layers.count + map.wms_layers.count
        %td.text-right
          = link_to add_multimap_path(@multimap, map: map.slug), method: :patch, class: 'btn btn-primary' do
            %i.fa.fa-plus
            Add map
