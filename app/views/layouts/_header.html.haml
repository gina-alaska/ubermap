%nav.navbar.navbar-default.navbar-static-top
  .container-fluid
    .navbar-header
      %button.navbar-toggle{ data: { toggle: 'collapse', target: '#header-navbar-collapse' }}
        %span.sr-only Toggle Navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      .navbar-brand Ubermap Manager
    #header-navbar-collapse.collapse.navbar-collapse
      %ul.nav.navbar-nav.navbar-right
        - if can? :manage, User
          %li= link_to 'Users', users_path
          
        - if signed_in?
          %li= link_to 'Logout', logout_path
        - else
          %li= link_to 'Login', login_path
        -# %li= link_to 'Organizations', organizations_path if can? :manage, Organization
